document.querySelectorAll(".task-status").forEach((project) => {const projectName=project.dataset.project;project.querySelectorAll(".task-btn").forEach((button)=>{const taskName=button.dataset.task;const storageKey=`eyrc-${projectName}-${taskName}`;if(localStorage.getItem(storageKey)==="completed") updateButton(button,true);button.addEventListener("click",()=>{const completed=!button.classList.contains("completed");updateButton(button,completed);localStorage.setItem(storageKey,completed?"completed":"pending");});});});function updateButton(button,completed){const state=button.querySelector(".task-state");button.classList.toggle("completed",completed);state.textContent=completed?"Completed ✓":"Pending";button.setAttribute("aria-label",`${button.dataset.task} ${completed?"completed":"pending"}`)}